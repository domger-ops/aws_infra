tables = {
    network = {
        tag = "network init"
    }
    service = {
        tag = "service init"
    }
    obs = {
        tag = "observability init"
    }
    devops = {
        tag = "devops init"
    }
}